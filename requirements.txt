# Thesis Grok Trader — minimal dependencies
# pip install -r requirements.txt

# Grok (xAI SDK — reused from ABC)
xai-sdk>=1.8.0

# Interactive Brokers (only needed when EXECUTION_BACKEND=ibkr)
ib-insync>=0.9.86,<1.0

# Config & logging
python-dotenv>=1.0.0,<2.0
pydantic>=2.10.0,<3.0
pydantic-settings>=2.6.0,<3.0
structlog>=24.4.0,<26.0

# Market data (MarketData.app + fallbacks)
httpx>=0.27.0,<1.0
numpy>=2.1.1,<3.0
pandas>=2.2.3,<3.0
yfinance>=0.2.44,<1.0
lxml>=5.0,<6.0
exchange-calendars>=4.5,<5.0

# Async
nest-asyncio>=1.6.0,<2.0
